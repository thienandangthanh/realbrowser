{
  "name": "realbrowser-skill",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "bin": {
    "realbrowser": "scripts/realbrowser.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --check scripts/realbrowser.mjs && node scripts/realbrowser.mjs self-test"
  }
}
